#include "sort.h"

/**
 * insertion_sort_list - C implementation of insertion sort algorithm
 * @list: Doubly linked list to sort
 *
*/

void insertion_sort_list(listint_t **list)
{
	listint_t *hold = list, *scan = list, *tmp;

    hold = hold->next;
    tmp = hold->n;
    

}
